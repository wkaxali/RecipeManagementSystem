<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class test extends Controller
{
  //  this is for adding up the tables data to a variable and post it to data base
    /*foreach($request->input('chk', []) as $key => $chk){
        $att = new Attendance;
        $att->student_id = $key;
        $att->date = $request->att_day;
        $status = $chk == 'true' ? 1 : 0;
        $att->status = $status;*/
     
     //}
}
